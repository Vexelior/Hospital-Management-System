@model IEnumerable<Core.Entities.Practice>

@{
    ViewData["Title"] = "Practices";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <h1>Practices</h1>
    <a class="btn btn-secondary text-light" asp-action="Create">Create New Practice</a>
    <div class="row">
        <div class="col-md-12">
            @if (Model != null && Model.Any())
            {
                <table class="table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Location</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var practice in Model)
                        {
                            <tr>
                                <td>@practice.Name</td>
                                <td>@practice.Location</td>
                                <td class="text-end">
                                    <a class="me-2" title="Edit" asp-action="Edit" asp-route-id="@practice.Id">
                                        <i class="fas fa-edit text-warning"></i>
                                    </a>
                                    <a class="me-2" title="Details" asp-action="Details" asp-route-id="@practice.Id">
                                        <i class="fas fa-info-circle text-primary"></i>
                                    </a>
                                    <a class="me-2" title="Delete" asp-action="Delete" asp-route-id="@practice.Id">
                                        <i class="fas fa-trash text-danger"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="text-center">No practices found.</p>
            }
        </div>
    </div>
</div>