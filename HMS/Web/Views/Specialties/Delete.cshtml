@model Core.Entities.Specialty

@{
    ViewData["Title"] = "Delete Specialty";
}

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h3>Are you sure you want to delete this specialty?</h3>
            <div>
                <h4>Specialty</h4>
                <hr />
                <dl class="row">
                    <dt class="col-sm-2">
                        Name
                    </dt>
                    <dd class="col-sm-10">
                        @Model.Name
                    </dd>
                </dl>
                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <input type="submit" value="Delete" class="btn btn-danger" />
                    <a class="btn btn-secondary text-light" asp-action="Index">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>
